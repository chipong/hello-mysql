// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// ByteLanguage is an object representing the database table.
type ByteLanguage struct {
	ID   []byte      `boil:"id" json:"id" toml:"id" yaml:"id"`
	Name null.String `boil:"name" json:"name,omitempty" toml:"name" yaml:"name,omitempty"`

	R *byteLanguageR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L byteLanguageL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ByteLanguageColumns = struct {
	ID   string
	Name string
}{
	ID:   "id",
	Name: "name",
}

var ByteLanguageTableColumns = struct {
	ID   string
	Name string
}{
	ID:   "byte_languages.id",
	Name: "byte_languages.name",
}

// Generated where

var ByteLanguageWhere = struct {
	ID   whereHelper__byte
	Name whereHelpernull_String
}{
	ID:   whereHelper__byte{field: "`byte_languages`.`id`"},
	Name: whereHelpernull_String{field: "`byte_languages`.`name`"},
}

// ByteLanguageRels is where relationship names are stored.
var ByteLanguageRels = struct {
	BytePilots string
}{
	BytePilots: "BytePilots",
}

// byteLanguageR is where relationships are stored.
type byteLanguageR struct {
	BytePilots BytePilotSlice `boil:"BytePilots" json:"BytePilots" toml:"BytePilots" yaml:"BytePilots"`
}

// NewStruct creates a new relationship struct
func (*byteLanguageR) NewStruct() *byteLanguageR {
	return &byteLanguageR{}
}

func (r *byteLanguageR) GetBytePilots() BytePilotSlice {
	if r == nil {
		return nil
	}
	return r.BytePilots
}

// byteLanguageL is where Load methods for each relationship are stored.
type byteLanguageL struct{}

var (
	byteLanguageAllColumns            = []string{"id", "name"}
	byteLanguageColumnsWithoutDefault = []string{"id", "name"}
	byteLanguageColumnsWithDefault    = []string{}
	byteLanguagePrimaryKeyColumns     = []string{"id"}
	byteLanguageGeneratedColumns      = []string{}
)

type (
	// ByteLanguageSlice is an alias for a slice of pointers to ByteLanguage.
	// This should almost always be used instead of []ByteLanguage.
	ByteLanguageSlice []*ByteLanguage
	// ByteLanguageHook is the signature for custom ByteLanguage hook methods
	ByteLanguageHook func(context.Context, boil.ContextExecutor, *ByteLanguage) error

	byteLanguageQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	byteLanguageType                 = reflect.TypeOf(&ByteLanguage{})
	byteLanguageMapping              = queries.MakeStructMapping(byteLanguageType)
	byteLanguagePrimaryKeyMapping, _ = queries.BindMapping(byteLanguageType, byteLanguageMapping, byteLanguagePrimaryKeyColumns)
	byteLanguageInsertCacheMut       sync.RWMutex
	byteLanguageInsertCache          = make(map[string]insertCache)
	byteLanguageUpdateCacheMut       sync.RWMutex
	byteLanguageUpdateCache          = make(map[string]updateCache)
	byteLanguageUpsertCacheMut       sync.RWMutex
	byteLanguageUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var byteLanguageAfterSelectMu sync.Mutex
var byteLanguageAfterSelectHooks []ByteLanguageHook

var byteLanguageBeforeInsertMu sync.Mutex
var byteLanguageBeforeInsertHooks []ByteLanguageHook
var byteLanguageAfterInsertMu sync.Mutex
var byteLanguageAfterInsertHooks []ByteLanguageHook

var byteLanguageBeforeUpdateMu sync.Mutex
var byteLanguageBeforeUpdateHooks []ByteLanguageHook
var byteLanguageAfterUpdateMu sync.Mutex
var byteLanguageAfterUpdateHooks []ByteLanguageHook

var byteLanguageBeforeDeleteMu sync.Mutex
var byteLanguageBeforeDeleteHooks []ByteLanguageHook
var byteLanguageAfterDeleteMu sync.Mutex
var byteLanguageAfterDeleteHooks []ByteLanguageHook

var byteLanguageBeforeUpsertMu sync.Mutex
var byteLanguageBeforeUpsertHooks []ByteLanguageHook
var byteLanguageAfterUpsertMu sync.Mutex
var byteLanguageAfterUpsertHooks []ByteLanguageHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *ByteLanguage) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range byteLanguageAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *ByteLanguage) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range byteLanguageBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *ByteLanguage) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range byteLanguageAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *ByteLanguage) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range byteLanguageBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *ByteLanguage) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range byteLanguageAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *ByteLanguage) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range byteLanguageBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *ByteLanguage) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range byteLanguageAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *ByteLanguage) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range byteLanguageBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *ByteLanguage) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range byteLanguageAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddByteLanguageHook registers your hook function for all future operations.
func AddByteLanguageHook(hookPoint boil.HookPoint, byteLanguageHook ByteLanguageHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		byteLanguageAfterSelectMu.Lock()
		byteLanguageAfterSelectHooks = append(byteLanguageAfterSelectHooks, byteLanguageHook)
		byteLanguageAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		byteLanguageBeforeInsertMu.Lock()
		byteLanguageBeforeInsertHooks = append(byteLanguageBeforeInsertHooks, byteLanguageHook)
		byteLanguageBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		byteLanguageAfterInsertMu.Lock()
		byteLanguageAfterInsertHooks = append(byteLanguageAfterInsertHooks, byteLanguageHook)
		byteLanguageAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		byteLanguageBeforeUpdateMu.Lock()
		byteLanguageBeforeUpdateHooks = append(byteLanguageBeforeUpdateHooks, byteLanguageHook)
		byteLanguageBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		byteLanguageAfterUpdateMu.Lock()
		byteLanguageAfterUpdateHooks = append(byteLanguageAfterUpdateHooks, byteLanguageHook)
		byteLanguageAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		byteLanguageBeforeDeleteMu.Lock()
		byteLanguageBeforeDeleteHooks = append(byteLanguageBeforeDeleteHooks, byteLanguageHook)
		byteLanguageBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		byteLanguageAfterDeleteMu.Lock()
		byteLanguageAfterDeleteHooks = append(byteLanguageAfterDeleteHooks, byteLanguageHook)
		byteLanguageAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		byteLanguageBeforeUpsertMu.Lock()
		byteLanguageBeforeUpsertHooks = append(byteLanguageBeforeUpsertHooks, byteLanguageHook)
		byteLanguageBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		byteLanguageAfterUpsertMu.Lock()
		byteLanguageAfterUpsertHooks = append(byteLanguageAfterUpsertHooks, byteLanguageHook)
		byteLanguageAfterUpsertMu.Unlock()
	}
}

// One returns a single byteLanguage record from the query.
func (q byteLanguageQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ByteLanguage, error) {
	o := &ByteLanguage{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for byte_languages")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all ByteLanguage records from the query.
func (q byteLanguageQuery) All(ctx context.Context, exec boil.ContextExecutor) (ByteLanguageSlice, error) {
	var o []*ByteLanguage

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to ByteLanguage slice")
	}

	if len(byteLanguageAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all ByteLanguage records in the query.
func (q byteLanguageQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count byte_languages rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q byteLanguageQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if byte_languages exists")
	}

	return count > 0, nil
}

// BytePilots retrieves all the byte_pilot's BytePilots with an executor.
func (o *ByteLanguage) BytePilots(mods ...qm.QueryMod) bytePilotQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.InnerJoin("`byte_pilot_languages` on `byte_pilots`.`id` = `byte_pilot_languages`.`byte_pilot_id`"),
		qm.Where("`byte_pilot_languages`.`byte_language_id`=?", o.ID),
	)

	return BytePilots(queryMods...)
}

// LoadBytePilots allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (byteLanguageL) LoadBytePilots(ctx context.Context, e boil.ContextExecutor, singular bool, maybeByteLanguage interface{}, mods queries.Applicator) error {
	var slice []*ByteLanguage
	var object *ByteLanguage

	if singular {
		var ok bool
		object, ok = maybeByteLanguage.(*ByteLanguage)
		if !ok {
			object = new(ByteLanguage)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeByteLanguage)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeByteLanguage))
			}
		}
	} else {
		s, ok := maybeByteLanguage.(*[]*ByteLanguage)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeByteLanguage)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeByteLanguage))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &byteLanguageR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &byteLanguageR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.Select("`byte_pilots`.`id`, `byte_pilots`.`name`, `a`.`byte_language_id`"),
		qm.From("`byte_pilots`"),
		qm.InnerJoin("`byte_pilot_languages` as `a` on `byte_pilots`.`id` = `a`.`byte_pilot_id`"),
		qm.WhereIn("`a`.`byte_language_id` in ?", argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load byte_pilots")
	}

	var resultSlice []*BytePilot

	var localJoinCols [][]byte
	for results.Next() {
		one := new(BytePilot)
		var localJoinCol []byte

		err = results.Scan(&one.ID, &one.Name, &localJoinCol)
		if err != nil {
			return errors.Wrap(err, "failed to scan eager loaded results for byte_pilots")
		}
		if err = results.Err(); err != nil {
			return errors.Wrap(err, "failed to plebian-bind eager loaded slice byte_pilots")
		}

		resultSlice = append(resultSlice, one)
		localJoinCols = append(localJoinCols, localJoinCol)
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on byte_pilots")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for byte_pilots")
	}

	if len(bytePilotAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.BytePilots = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &bytePilotR{}
			}
			foreign.R.ByteLanguages = append(foreign.R.ByteLanguages, object)
		}
		return nil
	}

	for i, foreign := range resultSlice {
		localJoinCol := localJoinCols[i]
		for _, local := range slice {
			if queries.Equal(local.ID, localJoinCol) {
				local.R.BytePilots = append(local.R.BytePilots, foreign)
				if foreign.R == nil {
					foreign.R = &bytePilotR{}
				}
				foreign.R.ByteLanguages = append(foreign.R.ByteLanguages, local)
				break
			}
		}
	}

	return nil
}

// AddBytePilots adds the given related objects to the existing relationships
// of the byte_language, optionally inserting them as new records.
// Appends related to o.R.BytePilots.
// Sets related.R.ByteLanguages appropriately.
func (o *ByteLanguage) AddBytePilots(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*BytePilot) error {
	var err error
	for _, rel := range related {
		if insert {
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		}
	}

	for _, rel := range related {
		query := "insert into `byte_pilot_languages` (`byte_language_id`, `byte_pilot_id`) values (?, ?)"
		values := []interface{}{o.ID, rel.ID}

		if boil.IsDebug(ctx) {
			writer := boil.DebugWriterFrom(ctx)
			fmt.Fprintln(writer, query)
			fmt.Fprintln(writer, values)
		}
		_, err = exec.ExecContext(ctx, query, values...)
		if err != nil {
			return errors.Wrap(err, "failed to insert into join table")
		}
	}
	if o.R == nil {
		o.R = &byteLanguageR{
			BytePilots: related,
		}
	} else {
		o.R.BytePilots = append(o.R.BytePilots, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &bytePilotR{
				ByteLanguages: ByteLanguageSlice{o},
			}
		} else {
			rel.R.ByteLanguages = append(rel.R.ByteLanguages, o)
		}
	}
	return nil
}

// SetBytePilots removes all previously related items of the
// byte_language replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.ByteLanguages's BytePilots accordingly.
// Replaces o.R.BytePilots with related.
// Sets related.R.ByteLanguages's BytePilots accordingly.
func (o *ByteLanguage) SetBytePilots(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*BytePilot) error {
	query := "delete from `byte_pilot_languages` where `byte_language_id` = ?"
	values := []interface{}{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	removeBytePilotsFromByteLanguagesSlice(o, related)
	if o.R != nil {
		o.R.BytePilots = nil
	}

	return o.AddBytePilots(ctx, exec, insert, related...)
}

// RemoveBytePilots relationships from objects passed in.
// Removes related items from R.BytePilots (uses pointer comparison, removal does not keep order)
// Sets related.R.ByteLanguages.
func (o *ByteLanguage) RemoveBytePilots(ctx context.Context, exec boil.ContextExecutor, related ...*BytePilot) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	query := fmt.Sprintf(
		"delete from `byte_pilot_languages` where `byte_language_id` = ? and `byte_pilot_id` in (%s)",
		strmangle.Placeholders(dialect.UseIndexPlaceholders, len(related), 2, 1),
	)
	values := []interface{}{o.ID}
	for _, rel := range related {
		values = append(values, rel.ID)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err = exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}
	removeBytePilotsFromByteLanguagesSlice(o, related)
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.BytePilots {
			if rel != ri {
				continue
			}

			ln := len(o.R.BytePilots)
			if ln > 1 && i < ln-1 {
				o.R.BytePilots[i] = o.R.BytePilots[ln-1]
			}
			o.R.BytePilots = o.R.BytePilots[:ln-1]
			break
		}
	}

	return nil
}

func removeBytePilotsFromByteLanguagesSlice(o *ByteLanguage, related []*BytePilot) {
	for _, rel := range related {
		if rel.R == nil {
			continue
		}
		for i, ri := range rel.R.ByteLanguages {
			if !queries.Equal(o.ID, ri.ID) {
				continue
			}

			ln := len(rel.R.ByteLanguages)
			if ln > 1 && i < ln-1 {
				rel.R.ByteLanguages[i] = rel.R.ByteLanguages[ln-1]
			}
			rel.R.ByteLanguages = rel.R.ByteLanguages[:ln-1]
			break
		}
	}
}

// ByteLanguages retrieves all the records using an executor.
func ByteLanguages(mods ...qm.QueryMod) byteLanguageQuery {
	mods = append(mods, qm.From("`byte_languages`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`byte_languages`.*"})
	}

	return byteLanguageQuery{q}
}

// FindByteLanguage retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindByteLanguage(ctx context.Context, exec boil.ContextExecutor, iD []byte, selectCols ...string) (*ByteLanguage, error) {
	byteLanguageObj := &ByteLanguage{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `byte_languages` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, byteLanguageObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from byte_languages")
	}

	if err = byteLanguageObj.doAfterSelectHooks(ctx, exec); err != nil {
		return byteLanguageObj, err
	}

	return byteLanguageObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ByteLanguage) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no byte_languages provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(byteLanguageColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	byteLanguageInsertCacheMut.RLock()
	cache, cached := byteLanguageInsertCache[key]
	byteLanguageInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			byteLanguageAllColumns,
			byteLanguageColumnsWithDefault,
			byteLanguageColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(byteLanguageType, byteLanguageMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(byteLanguageType, byteLanguageMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `byte_languages` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `byte_languages` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `byte_languages` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, byteLanguagePrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into byte_languages")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for byte_languages")
	}

CacheNoHooks:
	if !cached {
		byteLanguageInsertCacheMut.Lock()
		byteLanguageInsertCache[key] = cache
		byteLanguageInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the ByteLanguage.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ByteLanguage) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	byteLanguageUpdateCacheMut.RLock()
	cache, cached := byteLanguageUpdateCache[key]
	byteLanguageUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			byteLanguageAllColumns,
			byteLanguagePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update byte_languages, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `byte_languages` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, byteLanguagePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(byteLanguageType, byteLanguageMapping, append(wl, byteLanguagePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update byte_languages row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for byte_languages")
	}

	if !cached {
		byteLanguageUpdateCacheMut.Lock()
		byteLanguageUpdateCache[key] = cache
		byteLanguageUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q byteLanguageQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for byte_languages")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for byte_languages")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ByteLanguageSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), byteLanguagePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `byte_languages` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, byteLanguagePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in byteLanguage slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all byteLanguage")
	}
	return rowsAff, nil
}

var mySQLByteLanguageUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ByteLanguage) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no byte_languages provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(byteLanguageColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLByteLanguageUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	byteLanguageUpsertCacheMut.RLock()
	cache, cached := byteLanguageUpsertCache[key]
	byteLanguageUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			byteLanguageAllColumns,
			byteLanguageColumnsWithDefault,
			byteLanguageColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			byteLanguageAllColumns,
			byteLanguagePrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert byte_languages, could not build update column list")
		}

		ret := strmangle.SetComplement(byteLanguageAllColumns, strmangle.SetIntersect(insert, update))

		cache.query = buildUpsertQueryMySQL(dialect, "`byte_languages`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `byte_languages` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(byteLanguageType, byteLanguageMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(byteLanguageType, byteLanguageMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for byte_languages")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(byteLanguageType, byteLanguageMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for byte_languages")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for byte_languages")
	}

CacheNoHooks:
	if !cached {
		byteLanguageUpsertCacheMut.Lock()
		byteLanguageUpsertCache[key] = cache
		byteLanguageUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single ByteLanguage record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ByteLanguage) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no ByteLanguage provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), byteLanguagePrimaryKeyMapping)
	sql := "DELETE FROM `byte_languages` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from byte_languages")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for byte_languages")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q byteLanguageQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no byteLanguageQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from byte_languages")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for byte_languages")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ByteLanguageSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(byteLanguageBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), byteLanguagePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `byte_languages` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, byteLanguagePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from byteLanguage slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for byte_languages")
	}

	if len(byteLanguageAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ByteLanguage) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindByteLanguage(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ByteLanguageSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ByteLanguageSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), byteLanguagePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `byte_languages`.* FROM `byte_languages` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, byteLanguagePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in ByteLanguageSlice")
	}

	*o = slice

	return nil
}

// ByteLanguageExists checks if the ByteLanguage row exists.
func ByteLanguageExists(ctx context.Context, exec boil.ContextExecutor, iD []byte) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `byte_languages` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if byte_languages exists")
	}

	return exists, nil
}

// Exists checks if the ByteLanguage row exists.
func (o *ByteLanguage) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return ByteLanguageExists(ctx, exec, o.ID)
}
